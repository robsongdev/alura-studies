import React from "react";

export default class Botao extends React.Component{
    render():React.ReactNode {
        return (
            <button>
                Botão
            </button>
        )
    }
}